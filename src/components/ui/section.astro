---
import { cn } from "@/lib/utils"

interface Props {
  id: string
  title?: string
  subtitle?: string | string[]
  class?: string
}

const { id, title, subtitle, class: className = "" } = Astro.props
---

<section id={id} class={cn("mx-auto w-full", className)}>
  <div class="flex flex-col gap-2 md:gap-3">
    {title
      ? <div class="text-center text-2xl md:text-3xl">{title}</div>
      : null
    }
    { subtitle
      ? <div class="pb-2 text-xs md:text-base text-center text-muted-foreground">
          { Array.isArray(subtitle)
              ? subtitle.map((s) => <p>{s}</p>)
              : subtitle
          }
        </div>
      : null
    }
    <slot />
  </div>
</section>
