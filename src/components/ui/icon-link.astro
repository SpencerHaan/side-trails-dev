---
import ReactIconLink from "./icon-link"

const DEFAULT_ICON_SIZE = 24

interface Props {
  to: string
  name: string
  size?: number
}

const { to, name, size = DEFAULT_ICON_SIZE } = Astro.props
---

<ReactIconLink to={to} name={name} size={size} client:load />
