---
import { cn } from '@/lib/utils'

interface Props {
  type: "round" | "square"
  style?: "tertiary" | "muted"
  class?: string
}

const { type, style = "muted", class: className = "" } = Astro.props
---
<div class={cn(
  "w-fit p-4 border border-dashed",
  type === "round" ? "rounded-full" : "rounded-xl",
  style === "tertiary" ? "bg-tertiary-foreground text-tertiary border-tertiary" : "bg-muted text-muted-foreground border-muted-foreground",
  className
)}>
  <slot />
</div>
